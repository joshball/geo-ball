// import * as React from 'react'
// import { css, target } from 'glamor'
// import { Button, ButtonGroup } from '@blueprintjs/core';









// export interface IRadioButtonGroupComponentProps {
//     buttons: Array<IRadioButtonGroupItem>
//     onButtonClicked: OnRadioButtonGroupItemClickedCb
// }


// export const RadioButtonGroupComponent = (props: IRadioButtonGroupComponentProps) => {
//     const buttons = props.buttons.map((item, i) => {
//         const onClick = (event: React.MouseEvent<HTMLElement>) => {
//             event.persist();
//             props.onButtonClicked(event, item);
//         }
//         return <Button key={i} name={item.name} icon={item.icon} onClick={onClick} active={item.active}>{item.name}</Button>
//     })
//     return (
//         <ButtonGroup minimal={false}>
//             {buttons}
//         </ButtonGroup>
//     );
// }
